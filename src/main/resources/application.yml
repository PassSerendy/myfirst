server:
  port: 8081

jwt:
  secret: SmartOrderingBackendSecretKeyForJwtHmacSha256AlgorithmMustBeLongEnough
  expiration: 86400

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smart_ordering?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: Alexander
    password: mkd123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: is_deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

